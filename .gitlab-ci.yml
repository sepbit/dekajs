image: node:latest

cache:
  paths:
    - node_modules/

test:
  script:
    - npm install
    - npm run fix-style
    - npm run check-style
    - node_modules/.bin/nyc node_modules/.bin/ava test/*.js

